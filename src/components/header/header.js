import { AppBar, Toolbar } from "@mui/material";
import React from "react";

export function Header() {

    return (
        <AppBar>
            <Toolbar>
                ToDo App
            </Toolbar>
        </AppBar>
    )
}